
Example 1:

Input: nums = [2,7,11,15], target = 9
Output: [0,1]
Explanation: Because nums[0] + nums[1] == 9, we return [0, 1].                 2 -> 0
Example 2:																		7 -> 				

Input: nums = [3,2,4], target = 6
Output: [1,2]
Example 3:

Input: nums = [3,3], target = 6
Output: [0,1]

  for(int i=0; i<n-1;i++){
      
	  for(int j=i+1; j<n;j++){
	  
	       if(arr[i]+arr[j] == target){
		    return new int[]{i,j};
		   }
	  
	  }
	  return new int[2]
  }
  

Map<Integer,Integer> map = new HashMap();
int x,y;

for(int i=0; i<nums.length; i++){

   x= nums[i];                                   2 
   y = target - x;                               7
   
   if(map.containsKey(y)){
     return new int[]{map.get(y),i};
   
   }
	nums[i],i)

}
return new int[2];